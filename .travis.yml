language: julia
os:
  - linux
  - osx
julia:
  - 0.6
  #- release #still runs 0.5
  #- nightly
notifications:
  email: true
# uncomment the following lines to override the default test script
#script:
#  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#  - julia --check-bounds=yes -e 'Pkg.clone(pwd()); Pkg.build("LeafAreaIndex"); Pkg.test("LeafAreaIndex"; coverage=true)'
after_success:
  - julia -e 'Pkg.add("Documenter")'
  - julia -e 'cd(Pkg.dir("PACKAGE_NAME")); include(joinpath("docs", "make.jl"))'
